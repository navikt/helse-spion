post:
  summary: Hent saker med refusjon for gitt person
  tags:
    - Sak
  requestBody:
    content:
      application/json:
        schema:
          type: object
          required:
            - identitetsnummer
            - securityToken
          properties:
            identitetsnummer:
              type: string
              description: Norsk identitetsnummer, kan være fødselsnummer eller d-nummer
            securityToken:
              type: string
  responses:
    '200':
      description: |-
        200 response
      content:
        application/json:
          examples:
            sak:
              $ref: '#/components/examples/sak-oppslag'
