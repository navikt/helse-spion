type: object
required:
  - ytelsesperioder
  - oppsummering
properties:
  oppsummering:
    $ref: '#/components/schemas/Oppsummering'
  ytelsesperioder:
    type: array
    items:
      $ref: '#/components/schemas/Ytelsesperiode'