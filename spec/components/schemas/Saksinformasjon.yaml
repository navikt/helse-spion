type: object
required:
  - person
  - arbeidsgiver
  - saksliste
properties:
  arbeidsgiver:
    $ref: '#/components/schemas/Arbeidsgiver'
  person:
    $ref: '#/components/schemas/Person'
  saksliste:
    type: array
    items:
      $ref: '#/components/schemas/Sak'

